---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Set up complete project structure with all configuration files.

Purpose: Establish the working skeleton that all subsequent phases build on.
Output: Runnable Streamlit app (placeholder UI) with proper project structure.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/2025-01-23-crypto-rsi-screener-spec.md

**Project structure from spec:**
```
crypto-rsi-screener/
├── .env                    # API keys (gitignored)
├── .env.example            # Template for env vars
├── .gitignore
├── requirements.txt
├── watchlist.json          # Coin watchlist config
├── app.py                  # Main Streamlit app
└── src/
    ├── __init__.py
    ├── coingecko.py        # CoinGecko API client (Phase 2)
    ├── rsi.py              # RSI calculation logic (Phase 3)
    └── charts.py           # Plotly chart builders (Phase 4)
```

**Dependencies needed:**
- streamlit
- plotly
- httpx (for CoinGecko API)
- python-dotenv (for .env loading)
- pandas (for data manipulation)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project structure and dependencies</name>
  <files>requirements.txt, src/__init__.py, src/coingecko.py, src/rsi.py, src/charts.py</files>
  <action>
    1. Create `requirements.txt` with:
       - streamlit>=1.30.0
       - plotly>=5.18.0
       - httpx>=0.26.0
       - python-dotenv>=1.0.0
       - pandas>=2.1.0

    2. Create `src/` directory with `__init__.py` (empty or minimal)

    3. Create placeholder module files with docstrings only (no implementation):
       - `src/coingecko.py` - docstring: "CoinGecko API client for fetching market data and OHLC prices."
       - `src/rsi.py` - docstring: "RSI calculation functions for daily and weekly timeframes."
       - `src/charts.py` - docstring: "Plotly chart builders for RSI scatter visualization."

    Keep placeholders minimal - just module docstrings, no class/function stubs.
  </action>
  <verify>ls -la src/ shows 4 files; cat requirements.txt shows all 5 dependencies</verify>
  <done>requirements.txt exists with 5 dependencies; src/ has __init__.py plus 3 module placeholders</done>
</task>

<task type="auto">
  <name>Task 2: Create configuration files</name>
  <files>.env.example, .gitignore, watchlist.json</files>
  <action>
    1. Create `.env.example`:
       ```
       COINGECKO_API_KEY=your_api_key_here
       ```

    2. Create `.gitignore` for Python + env:
       - .env
       - __pycache__/
       - *.pyc
       - .venv/
       - venv/
       - .idea/
       - .vscode/
       - *.egg-info/
       - dist/
       - build/
       - .planning/ (planning docs are separate concern)

    3. Create `watchlist.json` with 10 initial coins (mix of large caps and mid caps):
       ```json
       {
         "coins": [
           {"id": "bitcoin", "symbol": "BTC"},
           {"id": "ethereum", "symbol": "ETH"},
           {"id": "solana", "symbol": "SOL"},
           {"id": "cardano", "symbol": "ADA"},
           {"id": "avalanche-2", "symbol": "AVAX"},
           {"id": "polkadot", "symbol": "DOT"},
           {"id": "chainlink", "symbol": "LINK"},
           {"id": "polygon", "symbol": "MATIC"},
           {"id": "uniswap", "symbol": "UNI"},
           {"id": "aave", "symbol": "AAVE"}
         ]
       }
       ```
       Note: Using CoinGecko IDs (avalanche-2 not avalanche).
  </action>
  <verify>cat .env.example; cat .gitignore | head -5; cat watchlist.json | python -m json.tool</verify>
  <done>.env.example has API key placeholder; .gitignore excludes .env and __pycache__; watchlist.json valid JSON with 10 coins</done>
</task>

<task type="auto">
  <name>Task 3: Create placeholder Streamlit app</name>
  <files>app.py</files>
  <action>
    Create `app.py` with minimal Streamlit UI:

    1. Import streamlit, dotenv
    2. Load .env with dotenv (so API key available when implemented)
    3. Set page config (title: "Crypto RSI Screener", layout: "wide")
    4. Display title "Crypto RSI Screener"
    5. Display placeholder text: "Chart and analysis will appear here."
    6. Add disabled refresh button (placeholder)
    7. Display "Last updated: Not yet refreshed"

    Keep it minimal - just prove the app runs. No data fetching, no charts.
    Use st.button with disabled=True for refresh (shows intent without functionality).
  </action>
  <verify>python -m streamlit run app.py --server.headless true & sleep 3 && curl -s http://localhost:8501 | grep -q "Crypto RSI Screener" && echo "App running" || echo "App failed"; pkill -f streamlit</verify>
  <done>app.py runs with streamlit run app.py; displays title and placeholder content</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `pip install -r requirements.txt` succeeds (or `pip install -r requirements.txt --dry-run` to check)
- [ ] `python -c "import src"` succeeds (package importable)
- [ ] `python -m streamlit run app.py` starts without errors
- [ ] All files exist: requirements.txt, .env.example, .gitignore, watchlist.json, app.py, src/__init__.py
</verification>

<success_criteria>

- All 6 files created (requirements.txt, .env.example, .gitignore, watchlist.json, app.py, src/__init__.py)
- 3 placeholder modules in src/ (coingecko.py, rsi.py, charts.py)
- App runs with `streamlit run app.py`
- Dependencies installable
- Watchlist has 10 coins with valid CoinGecko IDs
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
